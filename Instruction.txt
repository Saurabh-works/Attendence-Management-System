for clone: git clone  https://github.com/Saurabh-works/Attendence-Management-System.git 

for new push request:
1. git status
2. git add .
3. git commit -m "message"
4. git status
5. git push -u origin main

for new pull request:
1. git pull origin main

for json management:

install json: npm install -g json-server
staring login server: json-server --watch login.json --port 5000



